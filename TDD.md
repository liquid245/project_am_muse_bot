# Project AM Muse

**Technical Design Document (TDD) — версия 8.0**  
**Brooch Catalog Project** (максимально упрощённая)

**Дата:** 17 марта 2026  
**Разработчик:** Georgy  
**Цель:** запустить за 1–2 вечера, минимум кода, только кнопки, один репозиторий, 0 руб/мес.

### 1. Структура репозитория (один репозиторий — brooch-catalog)

```
brooch-catalog/
├── Каталог/                  ← все товары и заказы
│   ├── 01-saved/             ← черновики и ошибки
│   ├── 02-available/         ← товары в продаже
│   ├── 03-orders/            ← активные заказы
│   └── 04-archive/           ← завершённые заказы
├── Сайт/                     ← движок сайта (HTML + Tailwind + JS)
├── Бот/                      ← движок бота (Python + aiogram)
├── docs/                     ← общая документация (TDD, README и т.д.)
├── catalog.json              ← генерируется ботом
└── README.md
```

### 2. Формат item.yaml (бот создаёт и обновляет сам)

```yaml
title: "Брошь 'Морской бриз'"
description: "Ручная работа из японского бисера..."
price: 1250
stock: 3
created_at: "2026-03-17"
updated_at: "2026-03-17"
```

Всё остальное (фото, папки) бот переименовывает и размещает автоматически.

### 3. Сортировка
- По полю `updated_at` (самые свежие изменения сверху).
- Админ может обновить `updated_at` кнопкой «Поднять в начало» — бот просто ставит текущее время.

### 4. Хостинг
- **Бот** — PythonAnywhere (самый простой деплой в мире).
- **Сайт** — GitHub Pages (автоматически из папки `Сайт/`).
- Всё бесплатно навсегда.

### 5. Клавиатуры (только кнопки — никаких команд)

**Для покупателя (постоянно внизу):**
- 🛍 Каталог
- 💬 Написать менеджеру

**Под каждой карточкой товара:**
- Заказать эту брошь

**Для админа (появляется автоматически):**
- Добавить товар
- Список товаров
- Активные заказы
- Готово

### 6. Процесс заказа
- Покупатель нажимает «Заказать эту брошь».
- Бот собирает данные (ФИО, телефон, адрес).
- Сохраняет заказ в `Каталог/03-orders/`.
- Сразу отправляет уведомление **тебе и менеджеру** в личку.

### 7. Уведомления
- Только при новом заказе (никаких ежедневных рассылок).
- Сообщение приходит и тебе, и менеджеру (добавляем user_id менеджера в код).

### 8. Что убрали для максимальной простоты
- Нет position / сортировки кнопками ↑↓
- Нет cache-busting
- Нет Render + UptimeRobot
- Нет отдельного бэкапа
- Нет ежедневных напоминаний
- Нет сложных клавиатур

### 9. Non-Functional (простые)
- Стоимость: 0 руб/мес
- Ответ бота: до 3 секунд
- Максимум 300 товаров
- Нагрузка: до 20 заказов в день на старте

### 10. Risks (честно)
- PythonAnywhere может упасть — одна кнопка Restart.
- Сайт обновляется через 5–10 минут (кэш GitHub).
- Если бот упадёт — покупатели не смогут заказать до перезапуска.

